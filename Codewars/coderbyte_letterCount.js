function LetterCount(str) {

  // code goes here
  var wordArray= str.split(' ');
  var letterRepeatArray= wordArray.map();
  function letterRepeat(str){

  }
  return wordSortArray;

}

// keep this function call here
LetterCount(readline());
